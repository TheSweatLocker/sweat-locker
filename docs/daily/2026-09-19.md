# 2026-09-19

## QA QUEUE — game detail screenshots (Andy, 8:28-8:30pm)

**v1.0.1 IS LIVE TO USERS RIGHT NOW.** Everything below is user-visible.

Screens reviewed: NYY @ ARI, SF @ LAD, KC @ PIT.

### Q1 — MARKET row blank on every MLB game  🔴 user-visible
Renders `Spread — · Total — · ML —/—`.
DB has the data: `close_spread` 16/16, `close_total` 16/16, `home_ml_close`
16/16 on 2026-09-19. `MLB_CTX_COLUMNS` (app/index.tsx:5375) already SELECTs
all three.
=> Not a missing-column bug. `ctx` is arriving null for MLB. Everything
ctx-derived blanks together (Q1-Q5) while Jerry's Read and External
Handicappers — which come from different fetches keyed by game_id — render
fine. That asymmetry is the tell.
Lookup is app/index.tsx:18170-18174, map built at :5444-5457.

### Q2 — Model Consensus tiles missing  🔴
Same root cause as Q1. MLB LensGrid (GameDetailV2.tsx:1598-1603) reads
panel_implied_margin / jerry_pred_* / projected_* / model_pred_* /
mc_probabilities — all present in DB and all in the SELECT.

NOTE: this is a DIFFERENT bug from the NCAAF MC/SP+ blank fixed today in
dc9335c5. That one genuinely was a SELECT omission. Do not conflate.

### Q3 — LINE MOVEMENT shows "— → —  no open"  🔴 TWO causes
1. ctx null (Q1).
2. Genuine data gap even once ctx is fixed: on 2026-09-19 MLB ctx has
   `open_spread` 4/16, `open_total` 4/16, `home_ml_open` 6/16,
   `away_ml_open` 4/16. 12 of 16 games never captured an opening line.
   Suspect fallout from the freeze_opening_lines trigger breaking every
   MLB game_context UPDATE 9/17-9/19 (fixed today, 20260919b).
   ACTION: confirm openings capture correctly on the 9/20 slate before
   calling this closed.

### Q4 — STAT PROJECTIONS renders header + subtitle, no content  🟡
### Q5 — MONEY FLOW renders header + subtitle, no content  🟡
Empty sections should collapse, not render a header over blank space. A
section with nothing in it reads as a broken app. Applies to any
ctx-derived section — gate on content presence the way Model Consensus
gates on hasAnyLensValue (GameDetailV2.tsx:571).

### Q6 — Jerry ML reads contain no moneyline rationale  🔴 SYSTEMIC
All 3 MLB reads with `call_market=ml` on 2026-09-19 argue a TOTAL instead.
Measured over the full long_read text:

  game_id     call        ml-language hits   total-language hits
  59c14a5981  ml/HOME     0                  2
  ee466ede63  ml/HOME     0                  2
  0f7b4e8f0c  ml/AWAY     0                  5   <- Andy's screenshot

0f7b4e8f0c is NYY @ ARI, displayed as "New York Yankees ML · 80%
confidence". Its closing paragraph: "The projection sits 7.2, a tick above
the closing total of 7.0 ... even tight scoring breaks over. Flip if
Arizona gets unexpected rest news."

That is an OVER thesis under an ML headline. 59c14a5981 does the same
("threading above the 7.5 close"). Only ee466ede63 closes on something
ML-shaped ("justify the favorite") and even that never says moneyline.

The read generator is not conditioning the narrative on call_market. The
"Flip if" / closer template appears to be totals-shaped by default.
ACTION: condition the thesis + closer on call_market; add a generation-time
assertion that an `ml` read contains ML-side language and no total-line
argument. This is a credibility issue — the pick and its stated reasoning
disagree in public.

### Q7 — placeholder text shipped as a user-facing read  🔴
KC @ PIT (dbab73eb54, call_market=pass, conv 0) renders as the entire
Jerry's Read body:
  "Model recomputed to Under 8.0. Fresh read regenerating."
Internal regeneration status should never be the user-visible read. Either
hold the prior read or render a proper empty state.
Also note the contradiction on screen: verdict PASS / 55% confidence while
the text says the model recomputed to Under 8.0.

### Q8 — doubleheader collides in the ctx map  🟡 (found while tracing Q1)
2026-09-19 has Detroit @ Chicago White Sox TWICE (abb3f111a0be68d4,
99889cbde736c28d) — a doubleheader. contextMap is keyed by team name
(app/index.tsx:5447-5448), so game 2 overwrites game 1. Tapping either
game shows whichever loaded last. Needs a game_id-first lookup.

### Q9 — reads never regenerate after the model moves  🔴🔴 WORST ONE
Andy: "was KC game always a pass or did it change?"

Answer: the READ never changed. All 16 MLB reads were generated between
10:56:19 and 11:02:37 ET and not one has been rewritten since.

But the MODEL did move — `primary_play_computed_at` = 16:59 ET on all of
them. And four reads are sitting on a placeholder that promised a
regeneration which never came:

  matchup            read (AM)              model now (16:59)
  MIN @ LAA   10:57  pass  conv 0    vs     OVER 7.5   COVERAGE conv 55
  KC  @ PIT   10:59  pass  conv 0    vs     UNDER 8.0  COVERAGE conv 83
  CHC @ CIN   11:01  OVER 9.0 conv63 vs     OVER 9.0   STRONG   conv 63
  DET @ CWS   11:02  pass  conv 0    vs     OVER 8.0   COVERAGE conv 55

short_read on all four is literally:
  "Model recomputed to <pick>. Fresh read regenerating."

So KC @ PIT has been showing users **"Pass · 55% confidence"** for ~9.5
hours while the model has held **Under 8.0 at conviction 83** the entire
time. That is a publishable play rendered as no-play.

Two distinct failures stacked:
1. The regeneration the placeholder promises never runs. Find the job that
   is supposed to pick these up; it either is not scheduled, not
   triggered by a recompute, or is failing silently.
2. The placeholder is user-facing. Even if regen were merely slow, the
   fallback must be the prior valid read or a real empty state — never
   internal status text. (Same issue as Q7; Q7 is the symptom, this is
   the mechanism.)

Also unexplained: the screen shows 55% confidence for KC @ PIT while
jerry_reads.conviction = 0 and primary_play conviction = 83. Neither
source is 55. Find where that number comes from.

---

## Shipped today

- `20260919b` freeze_opening_lines rewritten schema-agnostically. The
  trigger referenced OLD.open_home_ml, which MLB does not have (MLB uses
  home_ml_open), so EVERY MLB game_context UPDATE failed with 42703 from
  9/17 onward. Root cause of a large share of this week's weirdness.
- `20260919a` NFL props volume cap — Prop Jerry NFL crashed at 389 rows.
  Dropped LEAN + per-day cap 40. 389 -> 58.
- `dc9335c5` NCAAF Model Consensus MC + SP+ tiles were never SELECTed
  (third occurrence of this bug class). Plus NCAAF team-name resolution:
  audited all 255 ctx names, 19 mechanical variants now resolve in code,
  11 genuine synonyms in migration 20260919c (NOT YET APPLIED).
  Resolver rejected two dangerous matches in testing:
  'Houston Baptist Huskies'->'Houston' (+11.6 SP+, wrong school) and
  'Louisiana Monroe'->'Louisiana' (-6.6 vs ULM's real -29.3).
- NFL UTC->ET date drift REPAIRED. 55 games / 708 rows moved: ctx 55,
  props 303, legacy props 68, jerry 8, prop_jerry 273, results 49, plus 6
  exact-duplicate result rows deleted. Verified against ESPN kickoff
  timestamps (NE@SEA = 2026-09-10T00:20Z = Wed 9/9 8:20pm ET), which
  matches the 9/9 Week 1 anchor. nfl_game_results carried the identical
  drift with the date baked into its game_id, so it was not independent
  truth and moved too. 0 game_id/date mismatches remain.
- UFC tab `state_message` rewritten (live via sport_registry, no rebuild).

## Open

- **No OTA update channel.** expo-updates is NOT installed and app.json has
  no `updates` block. Every client fix — including Q1-Q5 and the NCAAF
  tile fix already committed — needs a full TestFlight build. Third
  SELECT-class bug this month; each costs a build cycle to fix a string.
  Recommend wiring EAS Update.
- Migration 20260919c needs applying, then rerun ncaaf_game_context so FIU
  picks up its -11.5 SP+ and that card fills in.
- NCAAF modeling backtest (3 weeks) — Andy deferred to after today.
