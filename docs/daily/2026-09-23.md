# 2026-09-23

## Morning state

MLB pipeline mid-run at 07:20 ET (started 06:00). Context scored 10:55
UTC, props written 11:00-11:08, lookback enriched 11:12,
`prop_ensemble_scorer` had not reached them yet — so today's
"1,520 props, all COVERAGE, conviction 0" is a pipeline in flight, not
a regression. I checked before raising it as one.

The lookback swap landed last night and is HEALTHY:

      2026-09-23  n=1000  l5 populated=203  conviction>0=0 (pre-scorer)
      2026-09-22  n=1000  l5 populated=180  conviction>0=92
      2026-09-21  n= 323  l5 populated= 42  conviction>0=23

Coverage is higher than either prior day, and spot reads return 10
games for every player on today's slate.

Yesterday (09-22) published selection: **21-24, 46.7% (n=45)** — first
day after the leak fix. One day, too small to read anything into.

## Done

- `mlb_player_game_log` wired into `mlb_grade_overnight.yml`. It had
  NOTHING scheduled after the backfill — a store that stops the day it
  is built is worse than none, because `backfill_prop_lookback` now
  reads it and a stale table means every L5/L10 on the card quietly
  thins out instead of failing. 7-day window, `--resume`, idempotent.
  Proven: 19 games in window, 10 already stored, 9 pulled, 257 rows,
  0 failures.

## Waiting on Andy

1. **Apply the MLB prop regrade** — 101 corrections, 18 on published
   tiers, lifetime published moves 63.6% -> 63.5% (-0.10pp). The clean
   08-20..09-02 window does not move at all.
   `python regrade_props_from_game_log.py --apply`
2. **Apply the UFC regrade** — 7 corrections, record 5-10 -> 10-5.
   `python grade_ufc_jerry_reads.py --regrade --backfill-days 90`
3. Ship the build.

## Open from yesterday

B32 CLV capture (68 of 26,040 MLB, 0 of 1,317 NFL) — highest leverage
item left. B9 UFC client filter, B20, B22 all still build-gated.
