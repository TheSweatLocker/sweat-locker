# 2026-09-25 (Friday)

## Morning audit

Pipeline ran clean: MLB reads 11:18 ET, Sharp/Sweat 11:27–11:28, NFL weekly card 11:48,
live tier records 12:06.

**LR shadow verify: PASS** — MLB 17/17 games carry a distinct `_lr_ml_shadow.p_home_win`
(0.174 → 0.879). No stale-shadow bug.

### Yesterday (09-24) graded

| surface | record | hit |
|---|---|---|
| game_read | 13-1 | 93% |
| sharp_card | 7-2 | 78% |
| prop_jerry | 48-53 | 48% |
| sweat_card | 0-2 | — |
| ledger | 0-2 | — |

Ledger 0-2 was `chalk_parlay` + `prime_teased_single`, the last day that kind shipped.

### Today (09-25) on the board

Sweat Card 14 · Sharp 18 · POTD 1 · Ledger 1 · Degen 1 · prop_jerry 109 ungraded (normal, games unplayed).
No NFL plays — Sunday slate, correctly absent. NCAAF 3 (Navy/Army/Clemson MLs).

Ledger shipped **one** `chalk_parlay +123 (2 legs)` — the `prime_teased_single` kill
(30bb62b9) is confirmed working in production.

---

## 🚨 P0 — orphaned prop receipts: ~619 published picks can never be graded

`public_receipts` prop rows inherit their result from `prop_jerry_reads` via
`source_id`. Those source rows are regenerated (delete + reinsert) and come back with
new ids, so the receipts point at ids that no longer exist. The link is broken
permanently.

Measured on 09-23: **240 of 241** ungraded prop receipts have a `source_id` absent
from `prop_jerry_reads`. Orphan ids sit *inside* the live id range for that date
(reads 108836..110133, orphans 108828..109193), which is what a regenerate looks like.

| date | prop_jerry_reads | public_receipts | receipts ungraded |
|---|---|---|---|
| 09-22 | 209 (0 ungraded) | 424 | 215 (51%) |
| 09-23 | 189 (0 ungraded) | 429 | 241 (56%) |
| 09-24 | 145 (0 ungraded) | 307 | 162 (53%) |

The grader itself is fine — `prop_jerry_reads` is 100% graded. Only the receipt copy is
stranded, and `hits_under` is worst hit (519 of the ungraded) because it is the largest
family.

Two things hid this:
1. `grade_public_receipts.py` reports the skip as `source_row_ungraded`, which
   conflates "row is ungraded" with "row does not exist". It is the second.
2. The workflow step is `continue-on-error: true`, so nothing ever went red.

**Recommended fix:** grade prop receipts on a natural key
(`sport, game_date, player_name, prop_type, pick_line, pick_side`) instead of
`source_id`. That survives regeneration. Also split the skip counter so
"not found" is distinguishable from "ungraded". Not applied — needs Andy's call.

Related: [[project_public_receipts_integrity_918]] is the P0 that this belongs to.

## 🟠 sweat_card receipts carry no price

0 of 65 `sweat_card` receipts since 09-20 have `pick_odds`; `sharp_card` is 84 of 88
(95%). The dashboard is the most-seen surface and its receipts cannot show what price
a user would have gotten. Grading is unaffected; ROI and "we show the receipts" are.

## 🟠 NFL weekly card cache key says `wNone`

`jerry_cache` key `nfl_weekly_card_2026_wNone` — the week number resolves to None.
Written 11:48 today. Worth checking whether the app keys off that string.

## 🟠 nhl_game_results.close_spread does not exist

`grade_public_receipts.py` logs `⚠ fetch 400 … column nhl_game_results.close_spread
does not exist`, so NHL indexes 0 result rows. Harmless today (NHL not publishing) but
it will silently block NHL grading the moment it goes live.

## Note — 35 game receipts "ungradeable: no final score"

In the 09-21..09-25 window, 35 of 70 game-market receipts are ungradeable for want of a
final score, 34 more have no result row. Expect most to settle as finals land; re-check
tomorrow rather than treating as a defect.

---

## Carried over

- **pickdawgz Friday re-check** — it was 6/16 on Thursday; Andy says they post the full
  slate by Friday. Today is the day to confirm the multi-date fix (209fa2aa) reaches
  full coverage.
- **OddsCrowd** — still 0 NFL picks and 0 money flow since 09-20. Needs the URL-scheme
  rework (client-render, month-name date slugs, `/upcoming/football` now serves MLS).
- **tsc baseline 1888** — down from 1914 after the palette fix (d362608d). The baseline
  hid a live user-visible bug; worth deciding whether to drive it down.
- Client-only fixes awaiting a build: team-stats black text, ledger labels, O/U prose,
  externals red/green threshold.
